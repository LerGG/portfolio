// Dummy component to showcase layout

import styled from "styled-components";

// Dummy component
export const Footer = styled.div`
  height: 50vh;
  width: 100vw;
  background: ${({ theme }) => theme.backgrounds.default};
  opacity: 1;
`;
